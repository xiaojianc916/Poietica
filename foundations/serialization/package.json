{
  "name": "@poietica/foundations-serialization",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/public-api.ts",
  "types": "./src/public-api.ts",
  "exports": {
    ".": "./src/public-api.ts",
    "./serializer": "./src/serializer.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint src",
    "lint:fix": "biome lint --write src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "uuid": "catalog:",
    "valibot": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
