-- 这条对话已经说过几句话。
--
-- 0010 把附件挂在「第几条用户消息」上，可发送的那一刻没有任何地方知道这个
-- 数：record_prompt 每一轮都跑，但它不计数。计数落在这一列上，由那条语句自己
-- 加一并交出序号 —— 一次往返，一条写路径，调用方不需要自己去数。
--
-- 对齐方向要从末尾数，这是对 0010 那段注释的更正。那里写的是「回放到的用户
-- 消息条数与最大轮次对不上就整批不认领」，而这一列是从这次迁移才开始计数的：
-- 一条已经聊过五轮的旧对话计数器是 0，回放出来却是六条用户消息，按那条规则
-- 它以后发的每一张图都永远认领不了。
--
-- 计数器 N 覆盖的必然是**最后** N 条用户消息，所以回放出 M 条时，turn = k 对应
-- 第 M - N + k 条。存量对话自动落位，不需要补偿代码，也不需要对齐迁移。
-- 认领方仍然要在 M < N 时整批放弃：那说明回放合并或丢掉了消息，宁可不显示，
-- 不许张冠李戴。

ALTER TABLE threads ADD COLUMN prompts INTEGER NOT NULL DEFAULT 0;